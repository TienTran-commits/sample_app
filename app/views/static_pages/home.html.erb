<% provide(:title, t("header.home_ref")) %>
<div class="center jumbotron">
  <h1><%= t "home.welcome_message"%></h1>

  <h2>
    <%# add all this to one & study testing level, TDD (test drive dev) (optional) %>
    <%= t("home.description_html", href: link_to(t("footer.tutorial_ref"), Settings.href.rails_tutorial))%>
  </h2>

  <%= link_to t("home.buttons.signup"), signup_path, class: "btn btn-lg btn-primary" %>

</div>

<%= link_to image_tag("rails.svg", alt: "Rails logo", width: "200px"), Settings.href.ruby_on_rails %>
